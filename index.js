function calcPercentage(number) {
  // Replace your total marks with 1100.
  return Math.round((number / 1100) * 100);
}
// Replace the marks you obtained with 776.
console.log(calcPercentage(776));
